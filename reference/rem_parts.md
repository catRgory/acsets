# Remove multiple parts (must be processed from largest to smallest)

Remove multiple parts (must be processed from largest to smallest)

## Usage

``` r
rem_parts(x, ...)
```

## Arguments

- x:

  An ACSet.

- ...:

  Arguments passed to methods.

## Value

The ACSet, invisibly.
